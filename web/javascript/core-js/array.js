/* To complete the exercise, fill in the TODO sections */

const colours = ['red', 'blue', 'white', 'black'];

// Assign the 'white' element to the result variable
const result = 'white';

// Add an element 'yellow' to the end of the array
//colours[ colours.length +1 ] = 'yellow';
colours.push('yellow');

// Remove the first element from the array, and assign to the element variable
const element = colours.shift();

//https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Array
